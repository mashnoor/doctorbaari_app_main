<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.doctorbaari.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <meta-data
            android:name="com.facebook.accountkit.ApplicationName"
            android:value="@string/app_name" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.accountkit.ClientToken"
            android:value="@string/account_kit_client_token" />

        <activity android:name="com.facebook.accountkit.ui.AccountKitActivity" />
        <activity
            android:name=".acvities.WelcomeActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".acvities.RegistrationActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".acvities.NewsfeedActivity" />
        <activity
            android:name=".acvities.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen"
            android:launchMode="singleInstance"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".acvities.LoginActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".acvities.SearchSubstitute"
            android:label="@string/title_activity_search_substitute"
            android:theme="@style/AppTheme" />
        <activity android:name=".acvities.PostPermanentJobActivity"></activity>
    </application>

</manifest>